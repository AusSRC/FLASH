- name: Create secrets directory
  file:
    path: /etc/flashdb
    state: directory
    owner: root
    group: root
    mode: "0700"

- name: Copy secrets into that location
  copy:
    src: "{{ env }}/secrets.env"
    dest: /etc/flashdb/secrets.env
    owner: root
    group: root
    mode: "0644"
